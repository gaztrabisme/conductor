# Harness Map

One row per harness a conductor run may start in. Written for the coordinator: read your row before composing, because every engine choice in SKILL.md §3 binds to what the row says this harness can do. Distilled from the harness research of 2026-09-16 (`~/Documents/Work/lab/ceric/harness-research.md`); every URL was fetched live that day, and a cell that reads MISSING stayed missing in that research. It is carried forward as MISSING, never guessed.

## The four things every run needs

Product names differ; a run needs the same four things everywhere, and conductor calls them:

1. **The instruction file.** The Markdown file the harness loads at session start, carrying the project's hard rules and the pointer to this skill. Ground reads it first. Known forms: `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`, `.github/copilot-instructions.md`.
2. **The skill folder.** Where `SKILL.md` directories live at user level and project level, and whether the harness reads the frontmatter `description` to decide when a skill auto-triggers. Five of the six harnesses below do; four of the six state in their own docs that they follow the agentskills.io Agent Skills standard (https://agentskills.io). This skill's description is written for that trigger.
3. **The dynamic workflow primitive.** How the harness spawns subagents, whether they run in parallel, any documented cap, and whether a scripted multi-stage pipeline exists. Only Claude Code documents one: the Workflow tool, opted in by the word "ultracode" or "dynamic workflow" in the prompt or a session toggle. Where no scripted pipeline exists, the coordinator is the pipeline: `references/workflow-patterns.md`.
4. **The session stop.** Whatever keeps the agent working until stated criteria hold. Claude Code has `/goal`; Copilot's autopilot is the nearest elsewhere and it is bounded by a continuation limit. Where the harness has none, the goal file's UAT column is the stop and Close runs it.

## The table

Cells cite sources by [S#]; the URL for each is in Sources below; all fetched 2026-09-16.

| Harness | Skill load path + format, and does it read frontmatter `description` to trigger | Session-start instruction file | Subagent / orchestration primitive | Multi-stage workflow primitive | Hooks | Session-stop / goal mechanism | Custom slash commands | Non-interactive invocation (prompt, sandbox, cwd flags) |
|---|---|---|---|---|---|---|---|---|
| Claude Code (Anthropic) | Agent Skills standard [S2]. Personal: `~/.claude/skills/<name>/SKILL.md`; project: `.claude/skills/<name>/SKILL.md`; also enterprise managed dir, nested subdirs, plugin dirs, and `--add-dir` dirs [S2]. Frontmatter `description` "helps Claude decide when to load the skill automatically"; a skill can also be invoked directly as `/skill-name` [S2]. | `CLAUDE.md` (plus `.claude/rules/` and auto memory), loaded at the start of every conversation [S3]. | Custom subagents: each runs in its own context window with its own system prompt, tools, and permissions; Claude delegates to a subagent when the task matches its description [S4]. Spawned via the Agent tool (the CLI reference speaks of "the Agent tool call that spawned" a subagent) [S9]. Parallelism options: subagents, background agents (agent view), agent teams, dynamic workflows [S5]. Documented cap: Workflow-tool per-run concurrent agents via `CLAUDE_CODE_WORKFLOW_MAX_CONCURRENT_AGENTS` (1–256) [S10]; a plain subagent-count cap: MISSING. | Yes — "dynamic workflows": a JavaScript script Claude writes that orchestrates many subagents through the Workflow tool; runs in the background; manageable and re-runnable from `/workflows`; saved scripts live in `.claude/workflows/` (project) or `~/.claude/workflows/` (personal) [S6]. Opt-in trigger: the "Ultracode keyword trigger" in `/config`; Alt+W/Option+W dismisses the per-prompt highlight [S6]. | Lifecycle hooks incl. `SessionStart`, `PreToolUse`, `PostToolUse`, `Notification`, `Stop`, `SessionEnd`, `PreCompact`, `CwdChanged`; configured in `~/.claude/settings.json` or project `.claude/settings.json` [S8]. | `/goal <criteria>`: Claude keeps starting new turns until a small fast model judges the completion condition met, judges it impossible, or a fatal error clears the goal [S7]. Changelog documents goal retries and check-ins [S10]. | Custom commands merged into skills: `.claude/skills/<name>/SKILL.md` (and legacy `.claude/commands/<name>.md`) create `/<name>` [S2]. `/goal` is built-in [S7]. | `claude -p "query"` (print mode) [S9]. Flags: `--output-format text\|json\|stream-json`, `--input-format stream-json`, `--max-turns`, `--permission-mode plan\|...\|bypassPermissions`, `--dangerously-skip-permissions`, `--add-dir`, `--bare` (skip discovery of hooks/skills/CLAUDE.md for scripted speed) [S9]. Working directory: launch directory; extra dirs via `--add-dir` [S9]. Sandbox: documented feature page exists (verified live, HTTP 200) [S11]; sandbox flag names: MISSING in this pass. |
| Codex CLI (OpenAI) | Agent Skills standard [S12]. Repo: `.agents/skills` scanned from CWD up to the repo root; user: `$HOME/.agents/skills`; admin: `/etc/codex/skills` [S12]. `SKILL.md` must include `name` and `description`; implicit invocation matches the prompt against `description` (`allow_implicit_invocation` default `true`); explicit invocation via `$skill-name` mention and the `/skills` picker [S12]. | `AGENTS.md`: global `~/.codex/AGENTS.md` (or `AGENTS.override.md`), then project chain from project root down to CWD (one file per directory; fallback names via `project_doc_fallback_filenames`) [S13]. | "Multi-agent tools" exist and default on: `features.multi_agent` (default true), `agents.default_subagent_model`, custom role declarations; the `auto_review` approval policy uses a reviewer subagent [S14]. Spawn tool name, parallel execution, caps: MISSING in fetched docs. | MISSING — no scripted/declarative multi-agent pipeline in the fetched official docs [S12, S14, S15]. | Hooks: `hooks.json` or inline `[hooks]` tables in `config.toml`; discovered at `~/.codex/hooks.json`, `~/.codex/config.toml`, `<repo>/.codex/hooks.json`, `<repo>/.codex/config.toml`; plugins can bundle hooks. Events: `PreToolUse`, `PermissionRequest`, `PostToolUse`, `PreCompact`, `PostCompact`, `UserPromptSubmit`, `SessionStart`, `SessionEnd`, `SubagentStart`, `SubagentStop`, `Stop`, `Interrupt`. Gated by `features.hooks` (`features.codex_hooks` deprecated alias); non-managed hooks must be trusted [S15, S14]. | MISSING. | Skills serve as the mechanism: `$skill-name` mention, `/skills` picker [S12]. A separate user-defined `/command` or prompt-file mechanism: MISSING in fetched docs. | `codex exec "prompt"` (alias `codex e`) for scripted/CI runs; streams progress to stderr, final message to stdout [S16, S17]. Flags: `--sandbox read-only\|workspace-write\|danger-full-access` (prefer `--add-dir` over `danger-full-access`), `-C`/`--cd <dir>`, `--add-dir`, `--ephemeral`, and `--dangerously-bypass-approvals-and-sandbox` (docs say use only in a dedicated sandbox VM) [S17]. |
| Gemini CLI (Google) | Agent Skills standard [S20]. Workspace: `.gemini/skills/` or `.agents/skills/`; user: `~/.gemini/skills/` or `~/.agents/skills/` (within a tier, the `.agents/skills/` alias wins) [S20]. At session start the name and `description` of every enabled skill are injected into the system prompt; when the task matches a skill's `description` the model calls the `activate_skill` tool, gated by a user consent prompt [S20]. | `GEMINI.md`: global `~/.gemini/GEMINI.md`, then hierarchical project files, concatenated and sent with every prompt [S21]. | Subagents are "specialized agents" exposed to the main agent as a tool of the same name; automatic delegation when a task matches expertise, or forced with `@<subagent>` at prompt start; built-ins include `codebase_investigator`; overrides in `settings.json` under `agents.overrides` (example shows `runConfig.maxTurns: 50`) [S22]. Parallel execution statement: MISSING; per-subagent `maxTurns` cap shown in example [S22]. | MISSING. | MISSING — no hooks page exists in the fetched `docs/cli/` or `docs/core/` listings of the official repo [S26, S27]. | MISSING. | Custom commands: user `~/.gemini/commands/`, project `<project-root>/.gemini/commands/`; project wins on name conflict [S23]. | `gemini -p "prompt"` (`-p`/`--prompt`; headless also triggered automatically in a non-TTY environment); `--output-format` for JSON and streaming-JSON (JSONL) output [S24]. Sandbox: `-s`/`--sandbox` flag or `GEMINI_SANDBOX=true\|docker\|podman\|sandbox-exec\|runsc\|lxc` (macOS Seatbelt profiles; Docker/Podman containers) [S25]. A `--cwd`-style flag: MISSING (CLI runs in the current directory). |
| opencode (SST) | SKILL.md folders [S28]. Project: `.opencode/skills/<name>/SKILL.md`; global: `~/.config/opencode/skills/`; also reads `.claude/skills/` and `.agents/skills/` (project and `~/.claude/`, `~/.agents/`) [S28]. Frontmatter `description` is required and drives discovery: skill names+descriptions are what the agent sees when deciding to load a skill (doc sections: "Understand discovery", "Recognize tool description") [S28]. | `AGENTS.md` — project root file and a global file; Claude Code compatibility noted; `/init` scans the repo and creates/updates `AGENTS.md` [S29]. Exact global path: MISSING on the page text fetched. | Two agent types: primary agents and subagents. Built-in subagents: general, explore, scout; invoked by the primary agent for matching tasks or manually by `@` mention; the general subagent is described as running "multiple units of work in parallel". Agents are configured as JSON or Markdown files with options (description, temperature, max steps, tools, permissions, model, top-p) [S30]. Caps: MISSING. | MISSING. | TypeScript plugins: a plugin returns a hooks object subscribing to events, e.g. `tool.execute.before` / `tool.execute.after`, plus chat/session event subscriptions; plugins load from local files and npm, and "all hooks run in sequence" [S31]. | MISSING. | Custom commands: markdown files in the `commands/` directory (frontmatter properties; content becomes the prompt template) or a `command` map in `opencode.json` (template, description, agent, model, subtask) [S32]. Exact command-directory paths: MISSING on the page text fetched. | `opencode run [message..]` runs a prompt non-interactively; `opencode serve` starts a headless server and `opencode run --attach http://localhost:4096 "..."` attaches to it; flags `--print-logs`, `--log-level`, `--pure` [S33]. Sandbox flag: MISSING (a permissions doc area exists in the site nav [S34]). A `--cwd`-style flag: MISSING. |
| GitHub Copilot CLI (GitHub) | Skills documented in the "Add agent skills" how-to: a `SKILL.md` plus optional resources; `.copilot/skills` shown as the location [S39]. Skills are also a Copilot-wide concept ("About agent skills", "Built-in skills" in docs) [S35]. Whether the CLI reads frontmatter `description` to auto-trigger: MISSING on the pages fetched. | Repository custom instructions: `.github/copilot-instructions.md` [S40]; the About page describes automatic context management [S35]. | "Custom agents" for the CLI, configured under `.copilot/agents/`, and an "Invoke custom agents" flow [S41, S35]. Subagent spawn tool name, parallelism, caps: MISSING in fetched docs (a "Parallel task execution" concept page exists in the site nav — observed 2026-09-16, not fetched). | MISSING. | "Use hooks" how-to; hooks directory `.copilot/hooks/` [S38]. Hook event names for the CLI: MISSING in fetched content (PreToolUse/PostToolUse/Session Lifecycle/User Prompt Submitted are documented for the Copilot SDK surface). | Autopilot mode: Copilot works through steps autonomously "until it determines the task is complete", until Ctrl+C, or until the continuation limit (default 5 automatic continuations, adjustable with `--max-autopilot-continues`); toggled with Shift+Tab [S37]. This is the nearest equivalent to a goal mechanism, but it is bounded by the continuation limit, not an arbitrary stated criterion. | Built-in slash commands (e.g. `/login`, `/sandbox`) [S34, S35]. A user-defined custom-command mechanism (like `.claude/commands`): MISSING in fetched docs. | `copilot -p "prompt"` (programmatic mode) [S35, S36]. Flags: `--allow-tool=[TOOLS...]`, `--allow-url=[URLs...]`, `--allow-all` (docs say avoid unless sandboxed), `--no-ask-user`, `-s` (shown in examples), `--max-autopilot-continues` [S36, S37]. Sandbox: `/sandbox enable` inside a session (local sandboxing), or cloud sandboxes [S35]. A `--cwd`-style flag: MISSING. |
| Grok Build (xAI) — official CLI exists | Skills: `./.grok/skills/` (walked up to the repo root), `~/.grok/skills/`, any enabled plugin's `skills/`, extra `[skills] paths` in `~/.grok/config.toml` [S45]. `SKILL.md` YAML frontmatter: `name`; `description` = "What it does and when to use it"; `when-to-use` = extra trigger phrases; plus `paths`, `allowed-tools`, `argument-hint`, `user-invocable`, `disable-model-invocation` [S45]. | `AGENTS.md` at repo root; also reads `Agents.md`, `AGENT.md`, `CLAUDE.md`, `Claude.md`, `CLAUDE.local.md`, and every `.md` in `.grok/rules/` (`.claude/rules/` and `.cursor/rules/` read for compatibility); global rules in `~/.grok/`; deeper files take precedence [S46]. | Subagents are independent child sessions with their own context that return a summary; built-in types `general-purpose`, `explore`, `plan`; add/override under `.grok/agents/` or `~/.grok/agents/`; managed via `/config-agents` (alias `/agents`); personas under `[subagents.personas]` [S47]. Parallel execution and caps: MISSING. | MISSING — the Grok Build features index has no workflow/orchestration page [S53]. | Hooks are a shell command or HTTP endpoint called on lifecycle events; JSON files at `~/.grok/hooks/*.json` and `<project>/.grok/hooks/*.json`; Claude Code (`.claude/settings.json`) and Cursor (`.cursor/hooks.json`) hook files are also read. Events seen: `SessionStart`, `SessionEnd`, `PreToolUse` (the only blocking event), `Stop`, `StopFailure`, `SubagentStart`, `SubagentStop` (full table in doc). Project hooks need trust: `/hooks-trust` or `--trust` [S48]. | MISSING — `Stop`/`StopFailure` are lifecycle hook events, not a keep-working-until-condition loop. | User-invocable skills appear as slash commands `/<skill-name>`; TUI also has built-in slash commands [S52, S45]. | `grok -p "prompt"` (`-p`/`--single`) [S50]. Flags: `--cwd <PATH>`, `--sandbox <PROFILE>` (profiles: `off`, `workspace`, `devbox`, `read-only`, `strict`…), `--always-approve` (alias `--yolo`), `--allow`/`--deny` permission rules, `--output-format plain\|json\|streaming-json`, `-s/--session-id`, `-r/--resume`, `-c/--continue`, `--no-auto-update`; accepts Claude Code flag aliases where they overlap (`--allowedTools`, `--dangerously-skip-permissions`, `--append-system-prompt`, …) [S50, S51, S49]. |

## Shared ground

What every harness in the table has:

- A Markdown instruction file loaded at session start: `CLAUDE.md` (Claude Code), `AGENTS.md` (Codex, opencode, Grok Build), `GEMINI.md` (Gemini CLI), `.github/copilot-instructions.md` (Copilot CLI) [S3, S13, S21, S29, S40, S46].
- A skill folder mechanism based on `SKILL.md` directories, with distinct user-level and project-level paths [S2, S12, S20, S28, S39, S45]. Claude Code, Codex, Gemini CLI, and opencode state in their own docs that they follow the agentskills.io Agent Skills standard; Grok Build and Copilot CLI document `SKILL.md` skills without naming the standard on the pages fetched.
- Five of the six tie triggering to the `SKILL.md` frontmatter `description`. The exception: Copilot CLI, where the description-trigger fact is MISSING on fetched pages.
- A headless one-shot CLI: `claude -p`, `codex exec`, `gemini -p`, `opencode run`, `copilot -p`, `grok -p` [S9, S16, S24, S33, S36, S50].
- Sandbox controls (flag or profile) on five of six; opencode has no OS-sandbox flag in the docs fetched (permissions docs instead).
- Some hook or lifecycle extension mechanism on five of six. The exception: Gemini CLI, where hooks are MISSING in the official docs fetched.

What only some have:

- Scripted/declarative multi-agent pipelines: only Claude Code's dynamic workflows [S6]. Every other harness: MISSING.
- A session-stop/goal loop: Claude Code `/goal` is the only unbounded criterion-driven mechanism [S7]. Copilot CLI autopilot is a bounded variant (stops at a continuation limit, default 5) [S37]. All others: MISSING.
- Documented concurrency caps: Claude Code's `CLAUDE_CODE_WORKFLOW_MAX_CONCURRENT_AGENTS` (1–256) for workflow fan-out [S10]; Gemini CLI's per-subagent `maxTurns` [S22]. The rest: MISSING.

## Unlisted harness

Before the first run in a harness not in the table, establish three facts from its official docs (URL + fetch date, else MISSING) and write them into the run's `grounded.md`:

1. **The instruction file.** Which file(s) it loads at session start, from which scopes, because that file must carry or point to this skill's install instructions.
2. **The subagent primitive.** Whether it spawns subagents, from what, in parallel, under what cap, and whether a scripted multi-stage pipeline exists. If none of it is documented, plan the run coordinator-pipelined (`references/workflow-patterns.md`).
3. **The stop mechanism, or the fact that there is none.** If none, the goal file's UAT column is the stop and Close runs it.

The research file's full rule adds two more: the skill load path and trigger, and the headless invocation form with its sandbox and working-directory flags. Establish those too before composing. What remained incomplete in the 2026-09-16 pass is recorded in that file's "Incomplete in this pass" section.

## Sources (all fetched 2026-09-16)

- S1: (reserved) Agent Skills standard: https://agentskills.io
- S2: https://code.claude.com/docs/en/skills.md
- S3: https://code.claude.com/docs/en/memory.md
- S4: https://code.claude.com/docs/en/sub-agents.md
- S5: https://code.claude.com/docs/llms.txt
- S6: https://code.claude.com/docs/en/workflows.md
- S7: https://code.claude.com/docs/en/goal.md
- S8: https://code.claude.com/docs/en/hooks-guide.md
- S9: https://code.claude.com/docs/en/cli-reference.md
- S10: https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md
- S11: https://code.claude.com/docs/en/sandboxing.md (verified live HTTP 200; content not extracted)
- S12: https://developers.openai.com/codex/skills.md
- S13: https://developers.openai.com/codex/guides/agents-md.md
- S14: https://developers.openai.com/codex/config-reference.md
- S15: https://developers.openai.com/codex/hooks.md
- S16: https://developers.openai.com/codex/noninteractive.md
- S17: https://learn.chatgpt.com/docs/developer-commands.md
- S20: https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/skills.md
- S21: https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/gemini-md.md
- S22: https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/core/subagents.md
- S23: https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/custom-commands.md
- S24: https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/headless.md
- S25: https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/sandbox.md
- S26: https://github.com/google-gemini/gemini-cli/tree/main/docs/cli
- S27: https://github.com/google-gemini/gemini-cli/tree/main/docs/core
- S28: https://opencode.ai/docs/skills/
- S29: https://opencode.ai/docs/rules/
- S30: https://opencode.ai/docs/agents/
- S31: https://opencode.ai/docs/plugins/
- S32: https://opencode.ai/docs/commands/
- S33: https://opencode.ai/docs/cli/
- S34: https://opencode.ai/docs
- S35: https://docs.github.com/en/copilot/concepts/agents/about-copilot-cli
- S36: https://docs.github.com/en/copilot/how-tos/copilot-cli/automate-copilot-cli/run-cli-programmatically
- S37: https://docs.github.com/en/copilot/concepts/agents/copilot-cli/autopilot
- S38: https://docs.github.com/en/copilot/how-tos/copilot-cli/customize-copilot/use-hooks
- S39: https://docs.github.com/en/copilot/how-tos/copilot-cli/customize-copilot/add-skills
- S40: https://docs.github.com/en/copilot/how-tos/copilot-cli/customize-copilot/add-custom-instructions
- S41: https://docs.github.com/en/copilot/how-tos/copilot-cli/customize-copilot/create-custom-agents-for-cli
- S44: https://docs.x.ai/build/overview.md
- S45: https://docs.x.ai/build/features/skills-plugins-marketplaces.md
- S46: https://docs.x.ai/build/features/project-rules.md
- S47: https://docs.x.ai/build/features/subagents.md
- S48: https://docs.x.ai/build/features/hooks.md
- S49: https://docs.x.ai/build/features/sandbox.md
- S50: https://docs.x.ai/build/cli/headless-scripting.md
- S51: https://docs.x.ai/build/cli/reference.md
- S52: https://docs.x.ai/build/modes-and-commands.md
- S53: https://docs.x.ai/sitemap.xml
